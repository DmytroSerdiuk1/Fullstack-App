<div class="is-flex is-justify-content-space-between is-align-content-center mt-5">
    <div class="">
        <h1 class="title is-3">Course {{course.title}}</h1>
        <small class="blockquote-footer">id: {{course.id}}</small>
    </div>
    <div class="is-flex flex-column">
        <h2 class="title mr-3 is-3">{{course.price}} <small>RUB</small></h2>
        <form action="/cart" method="POST">
            <input type="hidden" name="id" value="{{course.id}}">
            <button type="submit" class="button is-danger">Add Cart</button>
        </form>
    </div>
</div>
<img src="{{course.img}}" alt="" style="width: 400px;" class="image>